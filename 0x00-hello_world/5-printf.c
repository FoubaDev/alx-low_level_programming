#include <stdio.h>

/**
 * main - Principal function
 *
 * Description: prints "with proper grammar, but the outcome is a piece of
 * art,"
 *
 * Return: We should return 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
